{
  "name": "todo-example-segregating-model-for-frontend",
  "version": "0.0.1",
  "private": true,
  "description": "TODO application example for frontend architecture",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "front": "pnpm --filter @todo-example/front",
    "vue": "pnpm --filter @todo-example/vue",
    "react": "pnpm --filter @todo-example/react",
    "svelte": "pnpm --filter @todo-example/svelte",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": "25.2.1"
  },
  "packageManager": "pnpm@10.26.1"
}
